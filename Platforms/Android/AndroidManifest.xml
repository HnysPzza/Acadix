<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
	<!--
		allowBackup="false": the app stores academic task notes, profile data and progress in
		plaintext SharedPreferences. With backup enabled these can be extracted off-device via
		`adb backup` on some configurations, which is a privacy risk for a student app.
		Auth tokens live in SecureStorage and are unaffected either way.

		dataExtractionRules/fullBackupContent are declared for Android 12+ and 11-, so if backup
		is ever re-enabled the sensitive stores stay excluded by default.
	-->
	<application android:allowBackup="false"
				 android:dataExtractionRules="@xml/data_extraction_rules"
				 android:fullBackupContent="@xml/backup_rules"
				 android:icon="@mipmap/appicon"
				 android:supportsRtl="true"
				 android:label="Acadix"></application>
	<uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
	<uses-permission android:name="android.permission.INTERNET" />
	<uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
	<uses-permission android:name="android.permission.VIBRATE" />
</manifest>
